import { readGame } from './utils/file-utils';

const gameId = "75";
const gameState = readGame(gameId);

//const player = getPlayer(gameState.teams, "Bot2");

//console.log(player?.id);

//if (player) {
    //planBotProduction(gameState.colonyMap, gameState.taskForceMap, player, threatGraph, mapData.starMap, gameState.explorationData, gameState.turn);
//};